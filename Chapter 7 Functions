# 7.1 Numeric Functions
SELECT 
	ROUND(5.7345, 2), -- 四舍五入
    TRUNCATE(5.7345, 2),  -- 直接截断
    CEILING(5.2),  -- 向上取整
    FLOOR(5.9),  -- 向下取整
    ABS(-5.2),  -- 取绝对值
    RAND() -- Random取随机数
-- Google mysql numeric functions for more

